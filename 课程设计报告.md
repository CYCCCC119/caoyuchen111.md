# 制造智能技术课程设计报告

# 重型机械装配车间螺栓拧紧质量智能检测与工艺追溯系统

**课程名称**：制造智能技术

**学生姓名**：________

**学号**：________

**专业班级**：________

**指导教师**：________

**完成日期**：2026 年 9 月

---

## 摘要

螺栓拧紧是重型机械装配的核心工序，其质量直接影响整机的连接可靠性与安全性。传统人工抽检模式存在覆盖度低、判定一致性差、问题追溯困难、工艺优化滞后等痛点。本课程设计面向重型机械装配车间螺栓装配场景，运用 Vibe Coding 开发方法，设计并实现了一套 B/S 架构的螺栓拧紧质量智能检测与工艺追溯系统。

系统围绕「质量管控—缺陷预警—工艺追溯—持续优化」全业务链条，完整落地《制造智能技术》课程三大核心技术方向：**工业大数据预处理与特征工程**（对力矩—转角高频时序数据进行清洗、分段、特征提取与选择，实现 12 维特征向 8 维核心特征的降维）、**制造过程质量智能检测与控制**（基于随机森林分类模型实现合格、欠拧、过拧、滑牙、虚拧 5 类质量状态的自动判定，测试集准确率 99.67%）、**制造工艺追溯与参数优化**（基于 Pearson 关联分析挖掘工艺参数与质量结果的关联规律，输出最优参数推荐）。

系统采用 Vue3 + Element Plus + ECharts 前端、FastAPI 后端、scikit-learn 算法引擎的四层架构，提供 5 个可视化页面、8 个 RESTful 接口，可现场演示「拧紧数据上传→质量实时检测→缺陷自动预警→工艺全链追溯→参数优化推荐」的完整业务流程。全程使用 Git 版本管理与 AI 提示词追溯，保留完整开发过程档案。

**关键词**：制造智能；螺栓拧紧；质量检测；随机森林；工艺追溯；参数优化

---

## 目录

- 第 1 章 绪论
- 第 2 章 需求分析
- 第 3 章 系统总体设计
- 第 4 章 核心算法设计与实现
- 第 5 章 系统实现
- 第 6 章 系统测试
- 第 7 章 总结与展望
- 参考文献

---

# 第 1 章 绪论

## 1.1 项目背景与意义

重型机械装备（如工程机械、矿山机械、大型船舶动力装置）的装配过程中，螺栓连接是最基本、最广泛的连接方式，一台大型设备往往包含成千上万个螺栓连接点。螺栓连接的预紧力是否达标，直接决定连接副的夹紧力、防松性能与疲劳寿命。拧紧不足（欠拧）会导致连接松动、异响甚至脱落；拧紧过度（过拧）可能导致螺纹塑性变形甚至断裂；滑牙、虚拧等缺陷则会造成「假连接」，是重大安全事故的隐患。

传统螺栓装配质量管控主要依赖人工抽检与力矩扳手复验，存在以下四个核心痛点：

1. **覆盖度低**：人工抽检通常不足全量工序的 10%，大量缺陷可能漏检；
2. **一致性差**：判定依赖检验人员经验，不同人员、不同时段的判定标准难以统一；
3. **追溯困难**：质量记录以纸质为主，出现质量问题时难以快速定位到具体工位、批次与工艺参数；
4. **优化滞后**：工艺参数调整依赖经验，缺乏数据驱动的量化依据，迭代周期长。

随着工业大数据与人工智能技术在制造领域的深入应用，利用高频拧紧时序数据（力矩—转角曲线）实现质量状态的自动、全量、快速判定已成为可能。本系统将制造智能技术引入螺栓装配质量管控环节，具有明确的工程应用价值与现实意义。

## 1.2 研究目标与内容

本课程设计的总体目标是：运用 Vibe Coding 开发方法，实现一套完整 B/S 架构的可运行 Demo 系统，形成从数据采集、质量检测、缺陷预警到工艺追溯优化的完整业务闭环。

具体研究内容包括：

1. **数据构建**：基于螺栓拧紧「力矩—转角」力学模型，构建覆盖 5 类质量状态的自建模拟数据集（3000 条），并开源至 Hugging Face；
2. **特征工程**：实现力矩—转角时序数据的清洗、分段、特征提取与特征选择，构建标准化特征数据集；
3. **质量检测模型**：训练并对比随机森林、SVM、梯度提升等分类模型，实现 5 类质量状态的自动判定；
4. **工艺追溯与优化**：建立工艺参数—质量结果的关联分析模型，输出最优参数推荐；
5. **系统集成**：开发 FastAPI 后端与 Vue3 前端，打通完整业务流程，形成可运行 Demo。

## 1.3 开发方法与工具

本课程设计全程采用 **Vibe Coding** 开发范式，以开发者为主导、AI 工具为辅助，通过「任务拆解—约束说明—验收标准」的提示词工程方法，将复杂系统需求拆解为低耦合的独立子任务，逐模块开发、逐项验收。全程使用 Git 进行版本管理与过程档案记录，每个功能点独立提交，AI 交流提示词同步归档，保证开发过程可追溯。

主要开发工具链：DeepSeek-V4 等编程大模型（需求拆解、代码生成、问题排查）、Git + GitHub（版本管理与过程档案）、Hugging Face（数据集托管）。

## 1.4 论文章节安排

本报告共分七章：第 1 章介绍项目背景、目标与开发方法；第 2 章进行需求分析；第 3 章给出系统总体设计；第 4 章详述三大核心算法的设计与实现；第 5 章介绍系统前后端实现；第 6 章给出系统测试结果；第 7 章总结全文并展望后续工作。

---

# 第 2 章 需求分析

## 2.1 业务需求分析

系统面向重型机械装配车间的螺栓装配工序，覆盖工位操作工、质量检验员、工艺工程师、车间管理员四类用户角色：

- **工位操作工**：拧紧作业后实时获取质量判定结果，异常时即时预警，指导返工；
- **质量检验员**：批量查看拧紧质量统计，按批次、工位追溯质量记录，生成质量报表；
- **工艺工程师**：追溯质量影响因素，分析工艺参数与质量的关联规律，获取参数优化建议；
- **车间管理员**：全局查看整体质量趋势、缺陷分布、工位合格率，支撑管理决策。

## 2.2 功能需求分析

系统功能划分为四大板块，分别对应课程的三大核心技术方向：

### （1）数据接入与预处理功能

对应工业大数据预处理与特征工程技术。支持螺栓拧紧过程力矩—转角时序数据的批量导入与实时接入，自动完成数据清洗、分段、归一化与特征提取，输出标准化特征数据集。

### （2）拧紧质量智能检测功能

对应制造过程质量智能检测与控制技术。基于机器学习模型实现 5 类质量状态（合格、欠拧、过拧、滑牙、虚拧）的自动判定与缺陷分类，支持单条实时检测与批量离线检测两种模式，输出缺陷等级评估与置信度。

### （3）工艺全链路追溯功能

对应制造工艺追溯与参数优化技术。建立多维度工艺追溯体系，支持按螺栓规格、工位、操作人员、物料批次、工艺参数、时间范围的多条件组合追溯，实现质量问题影响范围快速排查，并提供最优工艺参数推荐。

### （4）基础数据与系统管理功能

实现设备台账、螺栓基础信息、工艺基准、人员组织等基础数据的维护管理。

## 2.3 非功能需求

1. **性能需求**：单条拧紧数据检测响应时间小于 500ms；页面首屏加载时间小于 2s；支持 10000 条以上批量数据处理；
2. **易用性需求**：界面符合工业端操作习惯，流程清晰，核心功能 3 步内可达；
3. **可扩展性需求**：模块化架构，支持后续新增缺陷类型、扩展优化算法；
4. **可靠性需求**：具备异常数据容错能力，输入数据格式错误时给出明确提示，不发生系统崩溃。

---

# 第 3 章 系统总体设计

## 3.1 系统总体架构

系统采用标准四层 B/S 架构，自上而下分别为：

```
┌─────────────────────────────────────────────────────────┐
│  前端展示层  Vue3 + Element Plus + ECharts（5 个页面）     │
│  监控大屏 / 质量检测 / 工艺追溯 / 参数优化 / 基础数据管理    │
└──────────────────────────┬──────────────────────────────┘
                           │ RESTful API（HTTP / JSON）
┌──────────────────────────▼──────────────────────────────┐
│  后端服务层  FastAPI + Pydantic（8 个接口）                 │
│  数据接入 / 质量检测 / 工艺追溯 / 参数优化 / 质量统计         │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│  算法引擎层  scikit-learn + Pandas + NumPy               │
│  特征工程模块 / 质量检测模块 / 工艺优化模块                  │
└──────────────────────────┬──────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────┐
│  数据存储层  MySQL（业务结构化数据）+ InfluxDB（时序数据）    │
│  （Demo 采用 CSV 文件仓库零依赖，生产可无缝替换）            │
└─────────────────────────────────────────────────────────┘
```

各层职责清晰、低耦合高内聚，通过 RESTful API 与标准数据契约衔接，支持前后端分离开发与独立部署。

## 3.2 技术栈选型

| 层级 | 技术选型 | 选型理由 |
|------|---------|---------|
| 前端展示层 | Vue3 + Element Plus + ECharts | 组件丰富、图表能力强、开发效率高 |
| 后端服务层 | FastAPI + Pydantic | Python 生态统一、接口开发快、自带交互文档 |
| 算法引擎层 | scikit-learn + Pandas + NumPy | 轻量易用、接口统一、无需高端算力 |
| 数据存储层 | MySQL 8.0 + InfluxDB 2.0 | 双库架构，分别适配结构化与时序数据 |
| 版本管理 | Git + GitHub | 全流程版本管控，符合过程档案要求 |

> 说明：为降低演示部署门槛，后端数据层采用 CSV 文件仓库（零外部数据库依赖、开箱即用），生产环境可无缝替换为 MySQL + InfluxDB（建表脚本见 `data/business/init.sql`）。

## 3.3 功能模块划分

系统后端划分为七个服务模块，各模块低耦合高内聚：

- **数据接入服务**：时序数据上传、解析、格式校验、批量导入；
- **特征工程服务**：数据预处理、自动特征提取、特征标准化；
- **质量检测服务**：调用算法模型、质量判定、缺陷分级、预警生成；
- **工艺追溯服务**：多条件查询、关联分析、影响范围排查；
- **参数优化服务**：参数关联分析、最优参数计算、优化方案生成；
- **基础数据管理服务**：设备、螺栓、人员、工艺基准数据管理。

## 3.4 数据库设计

### （1）MySQL 业务库

核心数据表如下：

| 表名 | 用途 | 关键字段 |
|------|------|---------|
| `bolt_info` | 螺栓基础信息 | 螺栓 ID、规格、等级、标准扭矩、对应工艺 |
| `workstation` | 工位信息 | 工位 ID、名称、设备编号、负责人 |
| `tightening_record` | 拧紧记录 | 记录 ID、螺栓 ID、工位 ID、操作人员、时间、质量结果、特征数据 |
| `process_param` | 工艺参数 | 参数 ID、规格、目标扭矩、转速、版本 |
| `quality_warning` | 质量预警 | 预警 ID、记录 ID、缺陷类型、预警级别、处理状态 |
| `user_info` | 用户信息 | 用户 ID、用户名、角色、密码哈希 |

### （2）InfluxDB 时序库

- 测量值：力矩（torque）、转角（angle）；
- 标签：拧紧记录 ID、设备 ID、螺栓规格；
- 存储策略：原始数据完整保留，支持按记录 ID 查询完整拧紧曲线。

---

# 第 4 章 核心算法设计与实现

本章详述三大算法模块的设计与实现，三者分别对应课程的三个核心技术方向，深度融入系统业务逻辑。

## 4.1 模块一：时序特征工程

### 4.1.1 数据来源

由于公开领域不存在同时标注「合格、欠拧、过拧、滑牙、虚拧」5 类质量状态的单一数据集，本项目核心训练数据采用**自建模拟数据集**：基于螺栓拧紧「力矩—转角」力学模型（空行程→贴合→弹性上升→保持/跌落四阶段），结合重型机械装配工艺规范，通过 Python 脚本生成 3000 条完整拧紧周期数据（采样频率 100 Hz），覆盖 4 种螺栓规格（M12/M16/M20/M24）、3 种性能等级（8.8/10.9/12.9）、3 档工具磨损与不同环境温度工况。

| 子集 | 样本数 | 说明 |
|------|--------|------|
| benchmark 基准集 | 1200 | 标准工况（合格 700 + 缺陷 500） |
| extended 扩展集 | 1800 | 扩展工况，扩充稀有缺陷样本 |

数据集已开源至 Hugging Face：https://huggingface.co/datasets/zerozero01/bolt-tightening-quality。

### 4.1.2 预处理流程

预处理流程由 `preprocess.py` 自动化执行，共五步：

1. **数据清洗**：去除拧紧过程前后的空行程段；基于一阶差分采用 3σ 准则识别并剔除跳变点，线性插值补全；连续缺失超过 5% 的序列弃用；
2. **特征提取**：从力矩—转角时序提取 12 维时域特征，包括力矩最大值/平均值/标准差、力矩峰值出现角度、贴合点扭矩、拧紧总转角、平均速率、上升斜率、力矩波动率、转角偏差量、最终扭矩、保持段波动率；
3. **特征选择**：计算各特征与质量标签的 Pearson 相关系数，剔除相关系数低于 0.1 的弱相关特征；对相关系数高于 0.97 的共线性特征对进行合并，最终保留 8 维核心特征；
4. **特征标准化**：对特征向量做 Z-score 标准化，标准化参数仅基于训练集统计量计算，验证集/测试集复用，避免数据泄露；
5. **数据集划分**：按 7:2:1 比例分层随机抽样（按质量标签分层），得到训练集 2100 条、验证集 600 条、测试集 300 条。

### 4.1.3 特征选择结果

最终保留的 8 维特征及其与标签的 Pearson 相关系数如下：

| 特征 | 物理含义 | 与标签相关系数 |
|------|---------|---------------|
| `final_torque` | 最终扭矩 | -0.473 |
| `avg_rate` | 平均拧紧速率 | -0.486 |
| `torque_peak_angle` | 力矩峰值出现角度 | -0.411 |
| `torque_mean` | 力矩平均值 | -0.395 |
| `angle_deviation` | 转角偏差量 | -0.307 |
| `rising_slope` | 上升斜率 | -0.325 |
| `total_angle` | 拧紧总转角 | 0.282 |
| `hold_fluctuation` | 保持段波动率 | 0.242 |

特征选择的物理意义合理：最终扭矩、平均速率等特征直接反映拧紧充分程度，与欠拧/过拧强相关；保持段波动率则对应滑牙、虚拧等异常工况的波动特征。

## 4.2 模块二：拧紧质量智能检测

### 4.2.1 模型设计

质量检测本质是一个 5 分类问题。系统以**随机森林（Random Forest）**作为主模型，通过网格搜索优化超参数（`n_estimators=100`、`class_weight=balanced`、`max_features=sqrt` 等），同时训练 SVM（RBF 核）与梯度提升（GradientBoosting）作为对比模型。

随机森林采用 Bagging 集成思想，通过多棵决策树投票降低过拟合风险，对特征尺度不敏感、抗噪声能力强，适合本项目小样本、多工况的检测场景。

### 4.2.2 模型训练与评估

模型在测试集（300 条，未参与训练）上的评估结果如下：

| 模型 | 测试集准确率 | 宏平均 F1 |
|------|------------|----------|
| **随机森林（主模型）** | **99.67%** | **0.9968** |
| 梯度提升（GBDT） | 99.33% | 0.9922 |
| SVM（RBF） | 96.67% | 0.9668 |

随机森林模型各类别的分类报告：

| 类别 | 精确率 | 召回率 | F1 |
|------|-------|-------|-----|
| 合格 | 0.99 | 1.00 | 1.00 |
| 欠拧 | 1.00 | 1.00 | 1.00 |
| 过拧 | 1.00 | 0.98 | 0.99 |
| 滑牙 | 1.00 | 1.00 | 1.00 |
| 虚拧 | 1.00 | 1.00 | 1.00 |

测试集混淆矩阵显示，仅 1 条「过拧」样本被误判为「合格」（过拧召回率 97.62%），其余类别全部正确分类，验证了模型对 5 类质量状态的高精度识别能力。

## 4.3 模块三：工艺追溯与参数优化

### 4.3.1 工艺追溯体系

系统建立「螺栓规格—物料批次—工位—工艺参数—质量结果」的全链路追溯体系，将每条拧紧记录与其对应的工艺参数（目标扭矩、转速、螺栓等级、工具磨损、环境温度）、设备信息、操作人员、批次信息关联，支持正向追踪（从工艺参数定位质量结果）与反向定位（从缺陷记录追溯工艺源头）。

### 4.3.2 关联分析

采用 Pearson 相关系数分析工艺参数与质量特征、缺陷率之间的关联规律。关键发现如下：

**参数—特征关联**（Top 5）：

| 工艺参数 | 质量特征 | 相关系数 |
|---------|---------|---------|
| 目标扭矩 target_torque | 最终扭矩 final_torque | 0.703 |
| 目标扭矩 target_torque | 拧紧总转角 total_angle | 0.700 |
| 目标扭矩 target_torque | 上升斜率 rising_slope | 0.644 |
| 目标扭矩 target_torque | 平均速率 avg_rate | 0.535 |
| 工具磨损 wear | 转角偏差 angle_deviation | -0.256 |

**参数—缺陷率关联**：

| 工艺参数 | 与缺陷率相关系数 | 物理含义 |
|---------|----------------|---------|
| 工具磨损 wear | 0.151 | 磨损加剧缺陷率上升 |
| 螺栓等级 grade | 0.151 | 高等级螺栓更易出现拧紧缺陷 |
| 转速 speed | 0.017 | 转速对缺陷率影响较弱 |
| 目标扭矩 target_torque | ~0 | 目标扭矩与缺陷率近似正交（影响的是特征值而非缺陷与否） |

### 4.3.3 参数优化

基于关联分析与多目标网格搜索，针对不同螺栓规格输出最优工艺参数推荐（如目标扭矩、推荐转速区间、工具磨损阈值、环境温度范围），实现「在保证拧紧质量合格的前提下，提升装配效率、延长工具寿命」的优化目标。前端「工艺参数优化」页面以优化前后合格率对比、关联热力图、Pareto 前沿散点、Top 5 候选配置表等形式呈现优化结果。

---

# 第 5 章 系统实现

## 5.1 后端服务实现

后端基于 FastAPI 构建，采用「路由层—服务层—算法层」分层结构，对接 `algorithms/` 三大算法模块，对外提供 8 个 RESTful 接口：

| 分类 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 质量检测 | POST | `/api/detect` | 单条曲线质量检测 |
| 质量检测 | POST | `/api/detect/batch` | 批量曲线质量检测 |
| 数据接入 | POST | `/api/ingest` | 单条曲线接入并检测 |
| 数据接入 | POST | `/api/ingest/file` | CSV 文件接入（angle,torque 两列） |
| 工艺追溯 | POST | `/api/trace` | 多条件组合追溯 |
| 工艺追溯 | GET | `/api/stats` | 全局质量统计 |
| 参数优化 | POST | `/api/optimize` | 最优工艺参数推荐 |
| 参数优化 | GET | `/api/correlation` | 参数—质量关联分析 |

后端采用 Pydantic v2 定义请求/响应契约（如 `CurveInput`、`DetectResponse`），响应中返回记录 ID、质量标签、标签名称、置信度、缺陷等级、各类别概率与特征向量。单条检测请求示例：

```bash
curl -X POST http://127.0.0.1:8000/api/detect \
  -H "Content-Type: application/json" \
  -d '{"spec":"M20","angle":[0,10,20,30,40,50,60],"torque":[3,40,120,240,300,315,320]}'
```

## 5.2 前端界面实现

前端采用 Vue3（Composition API + `<script setup>`）+ Element Plus + ECharts，通过 Vite 构建、Axios 请求、Vue Router 路由，实现 5 个核心页面：

| 页面 | 对接接口 | 核心能力 |
|------|---------|---------|
| 拧紧质量监控大屏 | `/api/stats` `/api/trace` | 合格率/缺陷分布/工位排行 + 实时滚动预警 + 质量趋势 |
| 质量检测分析 | `/api/detect` `/api/detect/batch` `/api/ingest/file` | 单条/批量检测 + 力矩—转角曲线 + 概率分布 |
| 工艺追溯查询 | `/api/trace` | 多条件组合检索 + 反向定位详情 |
| 工艺参数优化 | `/api/optimize` `/api/correlation` | 关联热力图 + Pareto 前沿 + 优化前后对比 |
| 基础数据管理 | 前端 localStorage 演示 CRUD | 螺栓/工位/工艺参数/人员增删改查 |

其中「拧紧质量监控大屏」采用独立深色工业风格，其余页面为 Element Plus 浅色主题。质量检测页支持三种输入方式（示例曲线、上传 CSV、手动输入），示例曲线提取自真实样本数据，保证演示结果可追溯、非伪造。

---

# 第 6 章 系统测试

## 6.1 算法性能测试

算法层测试结果已在第 4 章详述：随机森林主模型测试集准确率 99.67%、宏 F1 0.9968，优于 SVM（96.67%）与 GBDT（99.33%）。交叉验证宏 F1 0.9977，验证了模型的稳定性。参数—特征关联分析结果符合拧紧力学规律（如目标扭矩与最终扭矩强正相关），物理意义正确。

## 6.2 单元测试与联调测试

后端编写 21 个单元测试用例（Python `unittest`），覆盖三大算法模块与核心接口的正常场景、边界场景与异常场景，**21 个用例全部通过**。

前后端联调验证：启动后端（Uvicorn :8000）与前端（Vite :5173），经 Vite `/api` 代理访问后端 8 个接口均返回正确数据；批量检测 5 类样本，模型正确判定合格/欠拧/过拧/滑牙/虚拧五类；`npm run build` 构建成功（2240 模块）。系统满足「单条检测响应 <500ms、页面首屏 <2s」的性能需求，具备异常数据容错能力。

---

# 第 7 章 总结与展望

## 7.1 工作总结

本课程设计运用 Vibe Coding 开发方法，完成了「重型机械装配车间螺栓拧紧质量智能检测与工艺追溯系统」的设计与实现，主要成果如下：

1. **数据层面**：基于力学模型构建覆盖 5 类质量状态的 3000 条自建模拟数据集并开源，完成从原始时序数据到标准化特征数据集的全链路处理；
2. **算法层面**：实现时序特征工程（12 维→8 维）、质量智能检测（随机森林，测试准确率 99.67%）、工艺追溯与参数优化三大算法模块，完整覆盖课程三大核心技术方向；
3. **工程层面**：搭建 FastAPI 后端（8 接口）+ Vue3 前端（5 页面）的四层 B/S 架构，实现全流程可视化业务闭环；
4. **过程层面**：全程 Git 版本管理，保留 AI 提示词追溯与开发档案，21 个单元测试全部通过。

## 7.2 不足与展望

1. **数据真实性**：核心训练数据为自建模拟数据，虽符合力学规律，但与真实生产线数据仍有差距。后续可接入真实拧紧设备采集的生产数据，进一步提升模型泛化能力；
2. **模型能力**：当前采用传统机器学习模型，后续可引入深度学习时序模型（如 CNN/LSTM）直接对力矩—转角曲线端到端建模，挖掘更丰富的时序模式；
3. **工程完善**：数据存储层 Demo 采用 CSV 仓库，基础数据管理为前端 localStorage 演示模式，后续可接入 MySQL/InfluxDB 与用户权限体系，形成生产级系统；
4. **业务闭环**：可进一步对接 MES/ERP 系统，实现质量预警自动触发工单与工艺参数的闭环迭代。

---

## 参考文献

[1] 张智海，等. 制造智能技术基础[M]. 北京：清华大学出版社，2022.

[2] 周济. 智能制造与工业大数据技术发展现状与趋势[J]. 机械工程学报，2021, 57(10): 1-12.

[3] 王雪，等. 基于机器学习的螺栓拧紧质量检测方法研究[J]. 制造技术与机床，2023(05): 142-147.

[4] Leporowski B, et al. ML-Pipeline for the Quality Assessment of Screwdriving Processes[EB/OL]. ScienceDirect, 2024. https://www.sciencedirect.com/science/article/pii/S221282712400934X

[5] pyscrew Screw Driving Dataset[EB/OL]. https://github.com/nikolaiwest/pyscrew

[6] 国家基础学科公共科学数据中心. 螺栓紧固实验测试数据[EB/OL]. https://nbsdc.cn/general/dataDetail?id=6988b31b195d2616afb01c17&type=1

[7] M2/M3 螺钉力矩—角度曲线数据集[EB/OL]. MDPI Data, 2024. DOI: 10.3390/data9100115

[8] Pedregosa F, et al. Scikit-learn: Machine Learning in Python[J]. Journal of Machine Learning Research, 2011, 12: 2825-2830.

[9] 本项目自建数据集. bolt-tightening-quality[EB/OL]. https://huggingface.co/datasets/zerozero01/bolt-tightening-quality

[10] Git 官方文档[EB/OL]. https://git-scm.com/doc
