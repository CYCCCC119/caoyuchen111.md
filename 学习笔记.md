## 一、Vibe Coding 方法学习

### 1.1 核心方法论认知

Vibe Coding 是一种以 AI 辅助为核心的高效开发范式，核心是通过精准的人机协作，将开发需求转化为可运行代码。其本质不是依赖 AI 自动完成全部开发，而是以开发者为主导，用 AI 承担重复性编码、基础框架搭建、语法纠错等工作，开发者聚焦业务逻辑、架构设计与核心算法。

### 1.2 提示词工程（Prompt Engineering）

学习了面向开发场景的提示词设计方法，核心包含三个要素：

- **任务拆解**：将复杂的系统开发需求拆解为独立、低耦合的子任务，单个提示词只对应一个功能点，避免 AI 因需求过于宽泛产生逻辑混乱。例如将 “后端开发” 拆解为数据库接口开发、故障诊断 API、工单管理接口等独立任务。
- **约束说明**：明确技术栈、代码规范、架构约束、输出格式四大类约束。技术栈约束指定编程语言、框架与依赖版本；代码规范约束命名规则、注释要求、分层架构；架构约束指定模块边界与调用关系；输出格式约束返回代码的结构与注释形式。
- **验收标准**：为每个任务设定可量化的验收条件，包括功能正确性、边界场景处理、性能指标、异常处理要求，用于验证 AI 生成代码的有效性。

### 1.3 上下文工程（Context Engineering）

学习了提升 AI 代码生成一致性的上下文管理方法：

- **项目规则前置**：在项目中维护统一的规则文件，每次开发前让 AI 先读取项目规则，确保生成的代码符合整体架构与规范，避免不同模块代码风格、结构不一致。
- **代码上下文传递**：修改已有功能时，向 AI 传入完整的现有代码片段、数据结构、依赖关系，而非仅描述需求，避免 AI 生成不兼容的接口或重复定义变量。
- **全局状态同步**：定期向 AI 同步项目整体进度、已完成模块、待开发模块与依赖关系，保证开发逻辑的连贯性。

### 1.4 规格驱动开发流程

学习了标准化的 AI 辅助开发流程：需求文档→AI 制定开发计划→分步实现→逐步验收。

1. 先输出完整的需求规格说明书，明确功能边界与业务逻辑，作为开发的总依据
2. 让 AI 基于需求拆解为开发任务清单，排定开发顺序与依赖关系
3. 按模块逐个开发，完成一个验收一个，验收通过后再进入下一模块开发
4. 每完成一个大版本进行整体集成测试，验证模块间的兼容性

### 1.5 AI 代码的测试与审查

学习了 AI 生成代码的质量管控方法：

- **自动化测试**：每个功能模块开发完成后，同步让 AI 生成单元测试用例，覆盖正常场景、边界场景、异常场景三类用例，自动验证功能正确性。
- **人工审查边界**：核心业务逻辑、核心算法、架构设计、安全相关代码必须人工审查；基础脚手架、通用工具函数、简单 CRUD 接口可直接使用 AI 生成结果。
- **审查重点**：业务逻辑正确性、工业场景适配性、性能瓶颈、安全漏洞、代码可维护性。

### 1.6 调试与人工接管策略

学习了 AI 开发出现问题时的处理方法：

- **回滚机制**：依托 Git 版本控制，每个功能点对应独立提交，出现问题可快速回滚到上一稳定版本，避免问题扩散。
- **缩小改动**：定位问题时，缩小 AI 的修改范围，只针对问题代码片段进行修正，避免全量重写引入新的问题。
- **最小复现**：构造最小复现用例，将复杂问题简化后再交给 AI 排查，提升问题定位效率。
- **人工接管**：复杂业务逻辑、核心算法、架构设计等关键环节，以人工为主导，AI 仅做辅助生成，不盲目依赖 AI。

## 二、Git 原理与功能学习

### 2.1 Git 核心原理

Git 是分布式版本控制系统，核心设计原理如下：

1. **分布式架构**：每个开发者本地都拥有完整的版本仓库，不依赖中央服务器即可工作，远程仓库仅作为协作同步的节点。
2. **快照存储机制**：Git 每次提交保存的是文件的完整快照，而非差异对比；通过哈希指针复用未修改的文件，在保证版本完整性的同时提升存储效率。
3. **核心对象模型**：
   - Blob 对象：存储文件的二进制内容，通过哈希值唯一标识
   - Tree 对象：存储目录结构与 Blob 对象的引用关系
   - Commit 对象：存储提交信息与 Tree 对象指针，串联形成版本历史链
4. **三区域工作流**：工作区（Working Directory）→暂存区（Staging Area）→本地仓库（Repository），通过暂存区实现提交粒度的精准控制。

### 2.2 Git 核心功能

1. **基础版本控制**
   - 仓库初始化与克隆：`git init`初始化本地仓库，`git clone`克隆远程仓库
   - 提交操作：`git add`将文件加入暂存区，`git commit`提交到本地仓库
   - 状态查询：`git status`查看工作区状态，`git log`查看提交历史
2. **分支管理功能**
   - 分支创建与切换：`git branch`创建分支，`git checkout`/`git switch`切换分支
   - 分支合并：`git merge`合并分支，`git rebase`变基操作
   - 核心价值：支持多任务并行开发，特性分支相互隔离，不影响主分支稳定性
3. **远程协作功能**
   - 远程仓库关联：`git remote add`绑定本地仓库与远程仓库
   - 同步操作：`git push`推送本地提交到远程，`git pull`拉取并合并远程更新
   - 版本获取：`git fetch`获取远程更新但不自动合并，便于手动审核

### 2.3 本项目 Git 管理规范

结合课程设计要求，制定了项目 Git 规范：

- **分支策略**：采用简化 Git Flow，设置`main`（稳定版本）、`dev`（开发集成分支）、`feature/xxx`（功能分支）、`docs`（文档分支）四级分支。
- **提交规范**：采用`类型: 描述`的提交信息格式，类型包括`feat`（功能新增）、`fix`（问题修复）、`docs`（文档更新）、`test`（测试用例）、`refactor`（代码重构）；坚持小步提交，单个功能点对应一次提交。
- **过程档案管理**：每次开发对应的 Prompt 会话日志同步存入仓库`logs/prompt/`目录，与代码提交对应，保证开发过程可追溯。

## 三、AI 工具与技术选型学习

### 3.1 AI 开发工具选型

本次课程设计选用的 AI 工具链及学习收获如下：

1. **代码生成工具：豆包 MarsCode**
   - 核心功能：项目级代码理解、代码生成、报错定位与调试方案生成
   - 学习收获：掌握了通过自然语言生成 Python 后端代码、前端页面代码的方法，能够快速搭建基础 CRUD 接口与页面框架；能够基于现有代码快速排查依赖问题与逻辑错误。
   - 项目定位：作为主力编码辅助工具，承担基础代码生成、bug 修复、代码重构工作。
2. **方案与文档工具：豆包 AI**
   - 核心功能：需求分析、方案设计、文档撰写、内容润色
   - 学习收获：能够快速生成需求规格书、设计报告的框架与初稿，通过多轮对话优化方案逻辑；能够辅助进行选题调研与资料整理。
   - 项目定位：承担文档撰写、方案梳理、问题答疑工作。
3. **模型与数据工具：Hugging Face**
   - 核心功能：开源模型与数据集托管、模型推理、数据集加载
   - 学习收获：掌握了工业公开数据集的获取与预处理方法，能够快速加载预训练模型并进行推理调用。
   - 项目定位：提供算法模块所需的数据集与基础模型参考。

### 3.2 算法框架选型

针对工业设备运维场景，拟选用的算法技术栈：

1. **数据处理框架：Pandas + NumPy**
   - 用于工业时序数据的清洗、特征提取与统计分析
   - 选型理由：生态成熟，工业数据处理案例丰富，学习成本低
2. **机器学习框架：scikit-learn**
   - 用于故障诊断分类模型、寿命预测回归模型的构建与训练
   - 选型理由：轻量易用，接口统一，适合快速开发验证，满足课程设计的算法需求
3. **可视化框架：Matplotlib + ECharts**
   - 后端数据可视化用 Matplotlib，前端展示用 ECharts
   - 选型理由：前后端分离，适配 B/S 架构的展示需求

## 四、选题调研学习

### 4.1 选题方向确定

选定**离散制造车间设备预测性运维智能系统**作为课程设计选题，属于制造场景中的设备运维环节，符合课程场景要求。

### 4.2 场景业务调研

- **业务背景**：离散制造车间中，旋转设备（电机、轴承、泵体）的故障是导致非计划停机的主要原因，传统定期维护存在过度维护或维护不足的痛点。
- **业务流程**：设备数据采集→状态实时监控→故障智能诊断→寿命预测预警→运维工单生成→维修记录归档，形成完整业务闭环。
- **业务价值**：降低设备故障率，减少非计划停机，优化运维成本，沉淀运维知识。

### 4.3 技术方向映射

对应《制造智能技术》课程 3 个核心技术方向：

1. **工业物联网与感知技术**：对应课程工业物联网章节，在系统中实现设备运行数据的采集、传输与预处理，构建设备状态感知的数据基础。
2. **故障诊断与预测性维护（PHM）技术**：对应课程设备健康管理章节，在系统中实现基于振动数据的故障类型智能识别与剩余使用寿命预测，是系统的核心功能。
3. **工业大数据分析技术**：对应课程工业大数据章节，在系统中实现从原始时序数据中提取时域、频域特征，构建故障分类与预测的特征体系。

### 4.4 系统架构调研

系统采用 B/S 四层架构，符合课程架构完整要求：

- **前端展示层**：实现设备监控大屏、故障诊断页面、寿命预测页面、运维工单页面，支撑完整业务流程操作
- **后端服务层**：提供设备管理、数据接口、故障诊断、工单管理、权限管理五类服务，承载业务逻辑
- **算法引擎层**：包含特征工程、故障诊断、寿命预测三个算法模块，实现核心智能功能
- **数据存储层**：MySQL 存储业务结构化数据，InfluxDB 存储设备时序数据

### 4.5 数据资源调研

全部采用公开工业数据集，符合课程要求：

1. **CWRU 轴承故障数据集**：凯斯西储大学公开的标准轴承故障数据集，包含正常、内圈故障、外圈故障、滚动体故障等多种状态，是故障诊断领域的标准测试数据集。
2. **NASA 电池退化数据集**：NASA 公开的锂电池充放电循环退化数据集，可用于剩余使用寿命预测算法的验证。
3. **模拟生成数据**：基于工业场景特征生成设备日常运行数据，用于系统功能演示与压力测试。

## 五、学习总结

通过本阶段学习，系统掌握了 Vibe Coding 的开发方法论，理解了 Git 分布式版本控制的核心原理与工程化管理规范，完成了 AI 工具链与算法框架的选型调研，确定了符合课程要求的工业场景选题，并明确了技术路线、系统架构与数据来源。后续将按照 Vibe Coding 流程推进系统开发，全程使用 Git 管理版本与过程档案，按时完成课程设计全部交付内容。
