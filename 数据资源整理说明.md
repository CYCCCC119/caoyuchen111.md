## 一、数据资源整体规划

本项目按照三层架构体系组织全量数据，从上到下依次对应系统的数据接入层、算法引擎层、业务服务层，各层数据职责清晰、逐级加工，完整覆盖工业数据从原始采集到业务应用的全链路。

第一层为原始数据层，保留最原始的采集或生成数据，不做任何修改，作为数据溯源的基准；第二层为特征数据集层，是经过清洗、标准化、特征提取后的结构化数据，直接作为算法模型的输入；第三层为业务数据层，是支撑系统业务流程运行的结构化管理数据，与业务功能一一对应。

---

## 二、原始数据集来源详情

全部数据均采用公开标准数据集与模拟生成数据，不涉及企业真实生产数据，符合课程要求。按照用途分为三类，分别支撑模型训练、工况扩展、业务流程跑通。

### 2.1 公开螺栓拧紧基准数据集

采用国内高校制造实验室公开的螺栓拧紧工艺试验数据集，是装配质量检测领域通用的标准测试数据集。
数据覆盖 M12、M16、M20、M24 四种重型机械常用螺栓规格，每种规格包含不同目标扭矩、不同拧紧转速下的完整拧紧过程数据。每条数据包含时间序列下的力矩值、转角值两个核心采集量，采样频率为 100Hz。所有数据均附带专业人员标定的质量标签，分为合格、欠拧、过拧、滑牙、虚拧 5 类状态。
整体数据规模共包含 1200 条完整拧紧周期数据，其中合格样本 700 条，各类缺陷样本共 500 条。该数据集主要作为质量检测模型的核心训练与测试基准，用于验证算法的准确性与泛化能力。

### 2.2 工况扩展模拟生成数据

基于螺栓拧紧力学特性公式，结合重型机械装配实际工艺规范，通过 Python 脚本模拟生成扩展工况数据。
数据补充了公开数据集覆盖不足的边缘场景，包含不同螺栓性能等级、不同工具磨损状态、不同环境温度下的拧紧时序数据，同时扩充了小概率缺陷样本的数量与程度分级，提升模型在复杂工况下的鲁棒性。
整体共生成 1800 条模拟数据，覆盖更多工况组合与缺陷梯度。该数据集主要用于扩充训练样本规模，丰富系统演示场景，验证系统在不同生产条件下的适配性。

### 2.3 基础业务模拟数据

参照重型机械装配车间的真实组织架构与业务流程，模拟生成全套业务基础数据。
数据包含工位信息、设备台账、螺栓基础信息、工艺基准参数、人员信息、历史工单六大类，所有数据均贴合真实车间的业务规则，具备完整的关联关系，可直接支撑业务流程跑通。该数据集主要用于系统业务功能演示，实现工艺全链路追溯，满足完整业务闭环的演示需求。

---

## 三、数据预处理全流程

对应课程**工业大数据预处理技术**，采用标准化流水线处理，全程可复现，处理脚本统一存放于仓库`https://github.com/CYCCCC119/caoyuchen111.md/new/main`目录。

### 3.1 原始数据清洗

第一步进行无效段裁剪，去除拧紧过程前后的空行程段，只保留从贴合点到拧紧结束的有效工作段，剔除无效数据干扰。
第二步进行异常值处理，采用 3σ 准则识别采集过程中产生的跳变点、离群点，判定为异常值后予以剔除，再通过线性插值法补全对应位置的数据，保证时序的连续性。
第三步进行缺失值处理，对于连续缺失长度小于采样点总数 5% 的序列，采用线性插值补全；缺失长度超过 5% 的序列直接弃用，避免低质量数据影响模型效果。

### 3.2 数据标准化处理

采用 Z-score 标准化方法，对力矩、转角两类时序数据分别进行归一化处理，将不同量级、不同工况的数据统一到相同尺度。
标准化参数仅基于训练集的统计值计算，验证集与测试集复用相同参数，避免数据泄露问题。处理后的数据均值为 0、标准差为 1，消除量纲差异对模型训练的干扰，提升算法收敛速度与稳定性。

### 3.3 时序数据分段

按照单个拧紧周期为单位进行数据分段，每条分段数据对应一次完整的拧紧过程。
采用滑动窗口法结合拐点检测算法，自动识别拧紧周期的起始点与结束点，实现批量数据的自动分段，无需人工标注。分段后统一数据长度，对长度不足的序列采用末端补零策略，保证所有样本的时间维度一致，满足模型输入要求。

### 3.4 数据标注与对齐

将原始数据附带的质量标签与分段后的时序数据一一对应，建立样本 ID 与标签的唯一映射关系。
统一标签编码规则，采用数字编码对应 5 类质量状态：0 代表合格，1 代表欠拧，2 代表过拧，3 代表滑牙，4 代表虚拧。标注结果单独存储，与特征数据分离管理，便于后续标签调整与扩展。

---

## 四、特征工程实现方案

对应课程**工业大数据特征工程技术**，从非结构化的时序曲线中提取结构化特征向量，为质量检测模型提供标准输入。所有特征提取逻辑通过 Python 脚本实现，支持批量自动化处理。

### 4.1 时域核心特征提取

从力矩与转角时序曲线中提取 12 维核心统计特征，每个特征都对应明确的物理意义，能够反映拧紧过程的动态特性与质量状态。
具体包括：力矩最大值、力矩平均值、力矩标准差、力矩峰值出现时间、贴合点扭矩、拧紧总转角、拧紧平均速率、力矩上升斜率、力矩波动率、转角偏差量、最终扭矩值、扭矩保持段波动率。

### 4.2 特征选择与降维

采用相关性分析方法进行特征筛选，计算每个特征与质量标签的 Pearson 相关系数，剔除相关性低于 0.1 的弱相关特征，减少无效信息干扰。
同时计算特征间的共线性，对相关系数高于 0.9 的特征对进行合并，保留物理意义更明确的特征。最终保留 10 维核心特征，在保证模型效果的前提下降低特征维度，提升推理速度。

### 4.3 数据集划分

采用分层随机抽样方法，按照 7:2:1 的比例将完整数据集划分为训练集、验证集、测试集。
分层依据为质量标签类别，保证每个集合中各类别样本的占比与整体数据集一致，避免样本分布偏差影响模型效果。其中训练集用于模型拟合训练，验证集用于超参数调优与模型选择，测试集用于最终效果验证与性能评估。

---

## 五、数据入库与存储方案

按照系统双库架构设计，分类入库存储，满足不同场景的调用需求，对应方案的数据存储层设计。

### 5.1 MySQL 业务数据存储

所有结构化业务数据与特征数据集存入 MySQL 数据库。
螺栓基础信息、工位信息、工艺参数、用户信息等基础数据采用独立表存储，建立主键与外键关联，保证数据一致性。特征数据集单独建立特征表，每条记录对应一个样本，存储样本 ID、特征向量、标签、数据来源、工况信息。建立样本 ID、质量标签、工况类型的联合索引，大幅提升查询与检索速度。

### 5.2 InfluxDB 时序数据存储

原始拧紧力矩 - 转角时序数据存入 InfluxDB 时序数据库。
以每条拧紧记录为单位组织数据，测量值为力矩和转角，标签包括记录 ID、螺栓规格、工位 ID、设备编号。保留原始采样频率的完整数据，支持按记录 ID 查询完整拧紧曲线，用于前端可视化展示与结果回溯。设置永久保留策略，原始数据只追加不修改，满足全生命周期追溯需求。

### 5.3 数据同步与版本机制

采用批量入库的方式，数据预处理完成后统一写入数据库。业务数据支持实时增删改查，时序数据采用追加写入模式，保证数据的可追溯性。
建立数据版本号机制，每次数据集更新都生成新的版本号，记录变更内容与变更原因，支持历史版本回溯，符合 Git 全流程管理的要求。

---

## 六、数据质量管控机制

建立全流程数据质量管控机制，覆盖数据生成、预处理、入库全环节，保证数据的准确性与可用性。

- **入库校验机制**：数据入库前自动校验格式、长度、标签完整性，不符合规则的数据拒绝入库并记录异常日志，便于问题排查。
- **抽样复核机制**：定期对入库数据进行抽样人工复核，重点核查缺陷样本的标注准确性与特征提取的正确性，控制数据质量。
- **版本管理机制**：所有数据集与处理脚本都纳入 Git 版本管理，每次数据更新都对应独立提交，记录变更原因与内容，可追溯可回滚。
- **文档同步机制**：数据变更时同步更新数据说明文档，保证文档描述与实际数据一致，避免使用偏差。

---

## 七、仓库数据目录规范

所有数据资源在仓库中按统一目录存放，层级清晰、职责明确，便于管理与使用。

```
data/
├── raw/                # 原始数据集目录
│   ├── public/         # 公开基准数据集
│   └── simulate/       # 模拟生成数据集
├── processed/          # 预处理后的特征数据集
│   ├── train.csv       # 训练集
│   ├── val.csv         # 验证集
│   └── test.csv        # 测试集
├── business/           # 基础业务数据
│   └── init.sql        # 数据库初始化脚本
└── README.md           # 数据说明文档
```

使用说明：运行`https://github.com/CYCCCC119/caoyuchen111.md/new/main/run_preprocess.py`脚本可自动完成原始数据的预处理与特征提取，生成标准数据集；运行`business/init.sql`可完成业务数据库的初始化与基础数据导入，快速搭建运行环境。

## 数据整理总结

本阶段完成了从数据收集、清洗、特征提取到入库的全流程数据资源整理工作，完整落地了工业大数据预处理与特征工程技术，构建了标准化、可复用的螺栓拧紧质量数据集。数据规模与质量均满足课程设计要求，可为后续算法模块开发、后端服务开发与系统集成提供可靠的数据支撑。
